{
  "name": "solution",
  "version": "1.0.0",
  "description": "RestAPI",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json:server":"json-server --watch db.json"

  },
  "author": "Vaishnavan",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
